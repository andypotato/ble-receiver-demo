const config = {

  // Bluetooth UUID for controller service
  controllerUUID: process.env.CONTROLLER_UUID,

  // Controller number (0 = first)
  controllerNum: process.env.CONTROLLER_NUM
};

module.exports = config;
